rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Leads must be written only through a server-side API using Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
